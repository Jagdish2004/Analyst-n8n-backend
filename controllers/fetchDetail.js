const client = require("../connectDb");


const fetchHistory = async (req, res) => {
   
};

const fetchSessionHistory = async (req, res) => {
  // your logic
};

module.exports = { fetchHistory, fetchSessionHistory };
